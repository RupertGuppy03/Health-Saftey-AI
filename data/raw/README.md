# data/raw

The original source documents — WorkSafe NZ PDFs and the Health and Safety at
Work Act 2015. Drop new source PDFs in here.

These files are **immutable**: never edit them by hand. The ingestion pipeline
cleans them up programmatically and writes the results to `data/processed`.
