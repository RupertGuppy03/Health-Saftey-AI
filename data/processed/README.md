# data/processed

The cleaned text produced by the ingestion pipeline from the PDFs in
`data/raw` — headers, footers, page numbers and boilerplate stripped out.

These files are generated by code, so don't edit them by hand — re-run the
ingestion pipeline instead.
