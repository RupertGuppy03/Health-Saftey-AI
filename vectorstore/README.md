# vectorstore

The persisted ChromaDB vector store — the embedded document chunks the chatbot
searches at query time.

The contents are generated by the embeddings pipeline and are **not committed
to git** (they can be large). This README is kept so the folder still exists
after you clone the repo. Build the vector store locally by running the
ingestion + embeddings steps.
