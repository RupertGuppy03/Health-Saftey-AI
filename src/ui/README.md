# src/ui

The Streamlit chat interface — the ChatGPT-style web app users interact with.
It sends questions to the FastAPI backend (`src/api`) and displays the answers
and citations. The app entry point lives here.
