# tests

Automated tests for the project, run with `pytest`. The layout mirrors `src/`
so each part of the code has matching tests.

AI-quality evaluations (DeepEval + golden Q&A pairs) live in the `eval/`
subfolder.
