# notebooks

Personal scratch space for exploration — prompt testing, chunking experiments,
trying things out. The US-08 edge-case notebook is a repeatable evaluation harness;
its committed fixture and generated evidence live under `tests/eval/` and `docs/`.

Keep polished, reusable code in `src/`; use this folder for rough work.
